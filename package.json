{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "hyperdash-dev",
  "title": "hyperDASH (hd) Dev",
  "version": "0.3.0",
  "description": "hyperDASH (hd): Obsidian Projects & Tasks (Bases-compatible, tag-only todos).",
  "icon": "hd-icon.png",
  "author": "your-name",
  "license": "MIT",
  "categories": [
    "Productivity"
  ],
  "permissions": [
    "filesystem"
  ],
  "scripts": {
    "build": "esbuild src/browse.tsx --bundle --platform=node --format=cjs --external:@raycast/api --outfile=dist/browse.js",
    "dev": "ray develop || true"
  },
  "commands": [
    {
      "name": "browse",
      "title": "hyperDASH Dev: Projects & Tasks",
      "subtitle": "Bases-compatible",
      "description": "List Todos (tag-only) and Projects (Bases-compatible)",
      "mode": "view",
      "icon": "hd-icon.png",
      "path": "src/browse.tsx",
      "preferences": [
        {
          "name": "vaultPath",
          "type": "textfield",
          "required": true,
          "title": "Vault Path",
          "description": "Absolute path to your Obsidian vault folder",
          "placeholder": "/Users/ja/Obsidian/JA"
        },
        {
          "name": "basesTodoFile",
          "type": "textfield",
          "required": false,
          "title": "Bases HyperDASH File (optional)",
          "description": "Path to your Bases hyperdash.base file; used to auto-detect the todo tag and views",
          "placeholder": "/Volumes/SVND_A_013/ja/lab/notes/base/hyperdash.base"
        },
        {
          "name": "basesProjectFile",
          "type": "textfield",
          "required": false,
          "title": "Bases Project File (optional)",
          "description": "Path to your Bases project.base file; used to auto-detect the project tag",
          "placeholder": "/Users/ja/Obsidian/JA/tc/db/project.base"
        },
        {
          "name": "todoTag",
          "type": "textfield",
          "required": false,
          "title": "Todo Tag (fallback)",
          "description": "Used if Bases file is not provided or has no tags.contains(...)",
          "default": "todo"
        },
        {
          "name": "projectTag",
          "type": "textfield",
          "required": false,
          "title": "Project Tag (fallback)",
          "description": "Used if Bases file is not provided or has no tags.contains(...)",
          "default": "project"
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.83.0",
    "fast-glob": "^3.3.2",
    "gray-matter": "^4.0.3",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}