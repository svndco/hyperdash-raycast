# hyperDASH (hd): tag-only Todos (Bases compatible)

-  Set "Vault Path" in command preferences
-  Optional: set "Bases Todo File" (todo.base). If set, we read tags.contains("...") and use that tag for Todos.
-  Fallback "Todo Tag" and "Project Tag" are configurable.

